/*
To prevent the form from refreshing the page, call event.preventDefault() inside handleSubmit().
This prevents the browser from performing its default behavior when a form is submitted.
*/

/*
Function components in React take 'props' as an argument and 
you don't have to specify the 'this' keyword in the core of function.

export function(props){
    
}
*/

/*
If you need to know what text is currently in the box, then you can ask the <input />, possibly with some code like this:

let input = document.querySelector('input[type="text"]');
let typedText = input.value; // input.value will be equal to whatever text is currently in the text box.
*/